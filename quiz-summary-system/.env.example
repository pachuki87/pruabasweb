# Variables de entorno para el sistema de envío de resúmenes de cuestionarios
# Copia este archivo a .env y rellena los valores reales

# ===========================================
# CONFIGURACIÓN DE EMAIL (Nodemailer)
# ===========================================

# Servidor SMTP para envío de emails
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587

# Credenciales del email
EMAIL_USER=tu-email@institutolidera.com
EMAIL_PASS=tu-contraseña-o-app-password

# Email remitente (opcional, por defecto usa EMAIL_USER)
EMAIL_FROM="Instituto Lidera" <tu-email@institutolidera.com>

# ===========================================
# CONFIGURACIÓN DE WEBHOOK (Axios)
# ===========================================

# URL del webhook para enviar datos
WEBHOOK_URL=https://tu-webhook-endpoint.com/api/quiz-summary

# Token de autenticación para el webhook (opcional)
WEBHOOK_AUTH_TOKEN=tu-token-de-autenticacion

# ===========================================
# CONFIGURACIÓN DE SUPABASE
# ===========================================

# URL de Supabase
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave anónima de Supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-clave-anonima

# ===========================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ===========================================

# URL de la aplicación
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Entorno (development, production)
NODE_ENV=development

# ===========================================
# NOTAS IMPORTANTES
# ===========================================

# 1. Para Gmail, necesitarás usar una "App Password" si tienes 2FA activado
#    - Ve a la configuración de tu cuenta Google
#    - Activa la verificación en dos pasos
#    - Genera una contraseña de aplicación
#    - Usa esa contraseña en EMAIL_PASS

# 2. Para el webhook, asegúrate de que el endpoint:
#    - Acepte peticiones POST
#    - Maneje el formato JSON enviado
#    - Tenga la lógica para procesar los datos del cuestionario

# 3. En producción, usa variables de entorno reales y seguras
#    - Nunca commits este archivo con valores reales
#    - Usa secretos de tu plataforma de hosting (Vercel, Netlify, etc.)

# 4. Para probar el sistema localmente:
#    - Copia este archivo a .env
#    - Rellena los valores de prueba
#    - Reinicia el servidor de desarrollo

# ===========================================
# EJEMPLO DE CONFIGURACIÓN PARA PRUEBAS
# ===========================================

# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_USER=test@gmail.com
# EMAIL_PASS=tu-app-password
# EMAIL_FROM="Instituto Lidera" <test@gmail.com>
# WEBHOOK_URL=https://webhook.site/your-unique-url
# WEBHOOK_AUTH_TOKEN=test-token-123
# NEXT_PUBLIC_SUPABASE_URL=https://xyz.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# NODE_ENV=development
